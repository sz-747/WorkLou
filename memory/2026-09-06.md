# WorkLou Session Memory

## Decisions

- Keep provider calls to reusable volatile service facts: current wait time for every service and current capacity for accommodation. Durable profile facts belong to background online enrichment because repeating case-specific calls increases caseworker notes and documentation load.
- Treat Watershed as explicitly synthetic demo data and keep its seed aligned with the synthetic CSV rather than presenting fixture assumptions as real provider evidence.
- Keep Bright Data Web Unlocker out of the demo. A blocked provider page is a logged source failure; the demo only needs the updater and discovery background processes to run visibly.
- Finish the backend-to-Base44 bridge sequentially before beginning the Figma UI integration.
- Keep Postgres canonical and use two thin Base44 scheduled functions to call the existing protected updater and discovery routes. Activate the schedules only after both manual Base44 runs pass.
- Treat Base44 scheduler activation as optional for the hackathon UI handoff. The demo can show clearly labelled stored demo activity or manual-run progress; do not present an inactive schedule as live.
- Branch the Admin removal from `origin/main`, not `backend-skeleton-setup`, because the latter is 21 commits ahead and still under active development. This keeps the removal PR independent.
- Remove the Admin interface and its dedicated correction code. Keep the background updater, discovery, spreadsheet engine, shared change history, and legacy `admin_corrected` database values so existing data and backend tests remain valid.
- Build the temporary public landing page at `/welcome` so it remains isolated from `/` and the existing caseworker workflow while Base44 continues its current branch work.
- On `/welcome`, keep the dotted `No More Admin` title non-interactive. The `Let's help more women now.` button starts one continuous transition in which the title dots move right while becoming a butterfly, then temporarily opens the blank `/welcome/complete` route. Show the supplied transparent Lou's Place logo in the top-left.
- Refine `/welcome` with the transparent Lou's Place logo, an Instrument Sans `Let's help more women now.` glass CTA using Lou's Place colours, upward antenna-led background butterfly paths, and larger shaded particle dots so the formed butterfly reads clearly and dimensionally.
- Superseded 2026-09-06: the large `/welcome` particle butterfly originally assembled during the first half of its nearly straight flight. Its fixed right-facing, antenna-led form and the CTA's subtle orange, white, pale blue, and green glow remain.
- Complete the large particle butterfly within 260 ms of the CTA click, then keep it visible on a 3.6-second gentle climb-and-descent path. Derive its capped pitch from the path direction so the antenna leads without random wobble or rotation.

## Open Questions

- ✓ Resolved 2026-09-06: no website-fetching fallback is required for the demo; rejected direct requests are logged as source failures.
