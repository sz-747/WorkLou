# All optional — without these the app uses a local keyword parser and rule-based extraction.
# LLM_API_KEY: key for an OpenAI-compatible endpoint (delivered via Base44 secrets, not this file)
# LLM_BASE_URL: e.g. https://api.openai.com/v1
# LLM_MODEL: e.g. gpt-4o-mini
DATABASE_URL=postgresql://referrals:local_referrals_password@db:5432/referrals
