# Build Plan

The phases of the build, in order. One branch/session per phase.

## Phases

| # | Phase | Scope | Status |
|---|-------|-------|--------|
| 0 | Setup (this docs structure) | Docs scaffolding + session rules | Done |
| 1 | TBD | | Not started |
| 2 | TBD | | Not started |

<!-- Add a row per phase as they are defined. Status: Not started / In progress / In review / Done. -->

## Rules

- Each phase gets its own branch, cut from latest `main`.
- A phase is only "Done" after the user has manually tested it and merged the branch.
- Do not start the next phase in the same session that finished the previous one.
