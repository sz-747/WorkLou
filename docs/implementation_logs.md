# Implementation Logs — Lou's Place Casework Workflow Tool

Append-only. New entries at the TOP (below this header block). Never edit or delete past entries.

## Entry format

```
## YYYY-MM-DD — Phase N: <name>
- Branch: <branch name>
- Changes: <what changed>
- DB changes: <migrations / seed changes / none>
- Tests run: <what was tested and how>
- Result: <pass / pass with notes / fail>
- Known issues: <anything outstanding>
- Next phase: <what comes next>
```

---

## 2026-09-06 — Contribution medals and legacy navigation removal

- Changes: enlarged every contribution square from 12px to 16px; replaced native gray scrollbars with thin peach-orange tracks and thumbs; added a four-medal achievement gallery using the local Lou's Place logo and dimensional orange/blue treatments. Badge titles use black for contrast; badges tilt on hover and flip on click to show the earned date. Removed the old global My Work, Women and System tools navigation from the root layout and deleted its unused component so it cannot appear above any route.
- Verification: `npx tsc --noEmit` and `git diff --check` passed; source search confirmed the legacy root navigation component is no longer referenced.

## 2026-09-06 — Simplified Contributions page

- Changes: removed the This month breakdown card and The work behind every square card, including their unused view-model data and styles. The summary statistics and 365-day activity grid remain.
- Verification: `npx tsc --noEmit` passed.

## 2026-09-06 — Caseworker contribution activity

- Why: the dashboard should acknowledge the caseworker's impact without adding noise or gamified badges.
- Changes: added a compact Women helped widget beside the Today heading with today and month totals. Added Contributions to Hannah's account dropdown and a dedicated local-demo page with four summary statistics, a 365-day GitHub-style orange activity grid, hover details for date/referrals/follow-ups, and a monthly support breakdown.
- Data: stable synthetic local-demo activity; no production reporting claim and no database schema change.
- Verification: `npx tsc --noEmit` passed; `/today` and `/contributions` returned HTTP 200 with the women-helped widget, Contributions links, year-grid markup and day-level referral details; changed-file diff check passed.

## 2026-09-06 — Live extraction and ranking boundary check

- Requested check: run a real OpenRouter note extraction and rank the resulting case against the current Postgres services.
- OpenRouter result: blocked before the request. `LLM_BASE_URL`, `LLM_API_KEY`, `LLM_MODEL` and `OPENROUTER_API_KEY` are absent from the local process, Windows user and machine environments; the Base44 environment file declares no populated secrets. No fallback extraction was presented as a successful LLM test.
- Postgres result: passed independently with a synthetic structured context matching the intended intake. Six active services were evaluated. The top three were Watershed Women's Crisis Accommodation (66), Southside DFV Legal Centre (39), and NSW Domestic Violence Line (34), with matched criteria and provider-confirmation trade-offs returned for each.
- Bright Data: not invoked because this check ranked the records already in Postgres. Bright Data refreshes and discovers service records upstream; it does not calculate the referral ranking.

## 2026-09-06 — Intake-to-ranked-referral profile flow

- Why: after extraction, the caseworker needs one obvious path into the new person's referral decision page, with the case facts and ranking rationale visible together.
- Changes: renamed the extracted-information action to Review Top 3 Referrals and kept its exact new case ID through approval and redirect. The destination is now labelled as the person's referral profile; income, languages, children and pets lead the priority pills. The top three services use a side-by-side layout with an enlarged first result and smaller second and third results. Every card separates matched strengths from mismatches, stale facts and facts that need provider confirmation.
- Ranking: LLM extraction remains upstream. The referral order stays deterministic and auditable at 100 points: needs 40, eligibility 35, location 15 and evidence freshness 10.
- Verification: `npx tsc --noEmit` passed. `/clients/new` and the existing case's `/plan` returned HTTP 200; the plan response contained the referral profile, Top 3 heading, trade-off sections, and income, languages, children and pets. Changed-file diff check passed.

## 2026-09-06 — Simplified profile and local demo letter

- Why: profile actions competed for attention, the account menu appeared beneath profile content, and the ranked referral plan was difficult to find.
- Changes: lifted the shared shell above page content so account popovers overlap correctly; removed the scattered profile-header actions and duplicate Continue button; placed a prominent Top referrals card directly below Recent contact; moved the close action into a small Profile options card; added a polished synthetic Amira support referral PDF as a clickable local file.
- Demo target: local-first. The PDF is served from the local Next.js app and no Base44 connection is required.
- Verification: PDF is one page, contains the expected synthetic-demo label and Amira content, and its rendered page passed visual inspection. TypeScript passed; the live profile contained Top referrals and the PDF link without the old scattered action; profile and PDF routes returned HTTP 200 with `application/pdf` for the file.

## 2026-09-06 — Gmail copy-and-paste referral handoff

- Why: the demo needs a real, understandable email handoff without storing Gmail credentials or claiming that WorkLou sent an external message.
- Changes: the editable referral modal now copies the complete message body, opens Gmail Compose in a popup with the provider address and subject prefilled, and tells the worker to paste and send. The generated draft includes structured bullet sections plus Hannah's saved signature. After sending in Gmail, the worker explicitly marks the referral as sent so it appears in Follow-ups.
- External effects: WorkLou does not send the email or inspect Gmail; Gmail remains the sending surface.
- Verification: `npx tsc --noEmit` passed; `/shelters` returned HTTP 200 from the running local app; changed-file diff check passed.

## 2026-09-06 - Synthetic My Work follow-up example
- Branch: `backend-skeleton-setup`
- Changes: the idempotent seed now ensures CASE-2026-001 is named Amira, has an approved context, and has one sent Watershed referral due for follow-up today. The example includes a realistic prepared message and appears on both My Work and Follow-ups.
- DB changes: demo data only; no schema changes.
- Tests run: seed applied to the current local database. Broader verification deferred while visual tweaking continues.
- Result: pass.
- Known issues: none.
- Next phase: continue visual tweaks.

## 2026-09-06 - Case intake, scored service plan, and faster local navigation
- Branch: `backend-skeleton-setup`
- Changes: replaced Add Person with a two-panel notes-first intake. The large notes area occupies four shares of the page and the Postgres-aligned extracted fields occupy one share. Extraction now prepares deterministic service rankings immediately and prefetches the plan route. Rebuilt the plan as a person overview, critical-information glass pills, vertical top-three service cards with phone actions and visible fit and trade-off evidence, and a sticky completion rail. Referral emails now open in a review modal; approval records the referral as sent and places it in Follow-ups. Removed Quick Exit and generic footer links; client-scoped pages retain only the current person's name. Button-style links no longer use underlines. Cards use softer translucent glass surfaces and shadows. Added route-level loading feedback and enabled Turbopack for local development.
- DB changes: no schema changes. Existing case context, service attribute, referral, and follow-up tables are reused. Fit scoring is deterministic: need coverage 40%, eligibility 35%, location 15%, and evidence freshness 10%.
- Tests run: production build and TypeScript clean; matching 28/28, refer 25/25, and follow-up 21/21. Live synthetic browser flow passed: notes to extraction, five services ranked, top-three plan, prepared referral modal, approval, and appearance in Follow-ups. The exact synthetic case and cascaded rows were removed afterward. Warm route requests on the Windows dev server measured 0.76 to 0.89 seconds, down from about 2 seconds inside Docker; route loading feedback covers cold compilation.
- Result: pass.
- Known issues: the first request after a fresh development-server restart still compiles the route and is much slower than warm navigation. This is development-only behavior; a production build precompiles routes.
- Next phase: continue visual tweaks, then prepare the Base44 handoff.

## 2026-09-06 — Base44 native scheduler bridge prepared
- Branch: `project-status-overview`
- Changes: added two thin Base44/Deno wrapper functions (`runWorkLouUpdater`, `runWorkLouDiscovery`) with inactive daily automation definitions, shared runtime code, and a Base44 handoff prompt. The wrappers call the existing Next routes so Postgres remains canonical and the business logic is not duplicated. Both machine-triggered routes now fail closed behind `SCHEDULER_SECRET`; the bounded discovery schedule uses `limit=1`. Local compose sidecars send the same bearer token with a development-only default.
- DB changes: no schema changes. One authorized local scheduled updater smoke run completed and wrote its normal `updater_runs` record plus two review candidates; canonical service data was not auto-applied.
- Tests run: focused updater suite 22/22; Base44 JSONC and `.base44/environment.json` parsed successfully; Docker Compose configuration validated; production build passed; live HTTP checks returned 401 for missing/wrong credentials and 200 for the correct token with updater status `completed`.
- Result: pass locally. The supplied Base44 functions and schedules still require branch visibility, secret configuration, deployment, two manual Base44 runs, and activation inside Base44.
- Known issues: Base44 GitHub sync normally reads `main`; the Base44 prompt must stop if it cannot access this feature branch. No live Bright Data discovery call was spent during the local bridge check.
- Next phase: Base44-side deployment and manual-run verification; then activate both schedules.

## 2026-09-06 — Verify call-list reduction and Watershed fixture reconciliation
- Branch: `project-status-overview`
- Changes: backend Verify grouping now sends only reusable volatile questions to the provider call list: current wait time for every service and current capacity for accommodation. Durable profile gaps such as pets, languages, visa and income remain online-updater/admin data work, so multiple client languages cannot create duplicate call questions. Duplicate operational rows collapse to one question and any current answer wins over older stale copies. The existing screen structure is unchanged apart from accurate empty-state wording. Watershed's synthetic seed now agrees with its demo CSV: pets are `negotiable` (case-by-case) and the stale wait-time value is `2-3 weeks`. During the full regression run, `db:test` was also isolated from the mutable demo case by giving it its own temporary case/context; product behavior was unchanged.
- DB changes: no schema changes. The current local demo row was reconciled to the same synthetic values (`pets=negotiable`, `wait_time=2-3 weeks`) and read back from Postgres.
- Tests run: all 12 database suites sequentially, 273/273 assertions green; `npm run build` completed with a fresh production `BUILD_ID`; live HTTP check on Watershed returned 200 with exactly two provider questions (wait time and capacity) and no pet/language question.
- Result: pass.
- Known issues: machine enrichment still depends on the existing scheduled updater; this change does not add Bright Data Web Unlocker or a new UI.
- Next phase: none.

## 2026-09-06 — Final demo hardening (integration + reliability only)
- Branch: `project-status-overview`
- Changes: **no new product features.** (1) Full five-step caseworker flow re-run live from fresh state (case workflow data reset per `demo_walkthrough.md`, Watershed pets fact reset to seeded unknown): Stage 1 extraction → field-source edits (urgency + safe contact method → worker observation) → save → approve; Stage 2 deterministic matches (Watershed, Southside + not-suitable reasons); Stage 3 Watershed pets confirmed provider-side ("welcome", Caseworker — phone) — propagated to Find support immediately; Stage 4 referral draft (LLM) → worker edit → mark sent (follow-up due 2026-09-12); Stage 5 provider response → follow-up draft (LLM) → outcome support_received (referral closed, forms correctly disappear); Stage 6 case note drafted (LLM, via the server action's exact lib path) and approved. DB writes inspected in Postgres at every stage; end-state matches the walkthrough table exactly. (2) Test-suite hardening: `src/db/test.ts` now creates its own confirmation fact (no reliance on / no mutation of the seeded Watershed pets fact); `src/db/test:matching` tolerates demo-added services (≥5, seeded five must be present) and the pets provider-confirmed end-state. (3) Dead code removed: `src/db/backfill-field-sources.ts`, `src/db/demo-followup.ts`, `db:backfill-sources` script. (4) All four docs rewritten for a fresh chat: statuses, schema, workflow, known limitations, demo steps, next steps.
- DB changes: none (schema untouched). Demo data reset then re-run to the documented completed end-state; stray `TEST-MATCH-*` rows from an earlier crashed test run deleted.
- Tests run: all 12 suites sequentially — **249/249 assertions green** (test 22, context 29, matching 24, verify 15, refer 23, followup 19, document 21, admin 16, updater 19, discovery 17, brightdata 10, spreadsheet 34). Live: updater manual run (7 checked, 3 fixture sources ok, 0 failed, 9 refreshes, 0 new candidates — idempotent); discovery manual run (20 results, 1 new candidate, 14 dedupe skips, 5 source failures logged); Excel staging intact (2 imported / 3 discarded from demo batch); no transmission code found (grep audit); all fixtures synthetic (example.org).
- Result: pass.
- Known issues: stage 6 of the fresh-state run was driven through the lib functions the server action wraps (same code path) rather than the UI buttons — the Document UI itself was live-tested in earlier phases. Discovery results vary run-to-run (live web) — by design, review queue only.
- Next phase: none — hackathon build complete. Final manual test checklist handed to the user; see `docs/build_plan.md` "Recommended next steps" for what to build next.

## 2026-09-05 — Phase 8 verification pass + discard-scope bug fix
- Branch: `project-status-overview`
- Changes: verification-only pass of Phase 8 against its acceptance criteria (end-to-end through the real admin forms, persistence checked in Postgres, logs scanned). **Bug found and fixed:** `discardStagedRow` (`src/lib/spreadsheet.ts`) never filtered by `stagedId` — its WHERE clause only checked `status='staged'`, so discarding ONE staged row discarded EVERY staged row across all batches (observed live: one click discarded 5 rows in the same millisecond). Fix: `and(eq(stagedServices.id, stagedId), inArray(stagedServices.status, ['staged']))`. Regression test added (`src/db/test-spreadsheet.ts`): a 2-row batch where discarding the first leaves the second staged.
- DB changes: none (schema untouched; verification artifacts removed — canonical demo data restored: 7 services, original staging batch only).
- Tests run: `npm run db:test:spreadsheet` — **34/34** (new per-row discard regression included). Fix verified live through the admin UI: two-row batch, discard one → other stayed staged. Live re-verified: upload through the real form, verbatim originals incl. unmapped columns, matched-merge outcome (Watershed: only empty website filled, all existing values kept, change-log entry), created service (Riverstone: excel provenance, needs flagged `needs_provider_confirmation`, change-log entries), decided rows render no decision forms + lib guard rejects re-decisions, export CSV reflects canonical changes, web logs 0 errors during the whole pass.
- Result: pass with notes — see the two pre-existing (non-Phase-8) issues below.
- Known issues: (1) `db:test:matching` shows 2 FAILs against the live demo DB — both from demo data accumulated in earlier phases, not Phase 8 code: it asserts exactly 5 seeded services with facts (the DB now has 7 — Women's Housing Company from the 7B live discovery merge + Riverstone from the 8 demo import), and Watershed's seeded pets fact was provider-confirmed during earlier Verify-stage demo testing (test expects `needs_provider_confirmation`). The suite's expectations assume a pristine seed DB; refreshing its expectations (or scoping it to seeded services) is advisable before the next phase. (2) The discard bug means any earlier multi-row discards performed before this fix wiped whole staging queues — no demo data was lost (the affected rows were verification cleanup).
- Next phase: none started (verification only, per request).

## 2026-09-05 — Phase 8: spreadsheet migration (staging → non-destructive import → CSV export)
- Branch: `project-status-overview`
- Changes: Excel migration compatibility so Lou's existing service list feeds the canonical Postgres schema without a forced cutover. New `src/lib/spreadsheet.ts`: RFC4180-ish CSV parser (quoted fields, doubled quotes, embedded commas/newlines, CRLF/LF) → header alias mapping (`Service name`/`Provider`→name, `What they help with`→needs, etc.) → plain-language need aliases → `mapSpreadsheetRow()`; `importSpreadsheetText()` stages every mapped row verbatim (`raw_values` keeps original cells incl. unmapped columns) matched by normalised name against canonical services; `importStagedRow()` is the human decision and **non-destructive by design** — new rows create services (`source_type='excel_import'`), matched rows only FILL empty core fields and add MISSING need facts (existing canonical values never overwritten, so better-verified data survives), added needs land as `needs_provider_confirmation`, every change appends to the service change log; `discardStagedRow()` records the decision with canonical untouched; `buildServicesCsv()`/`exportServicesCsv()` round-trip the canonical directory back to CSV. Admin UI: "Spreadsheet migration (staging)" section — CSV upload (+ name), staged-row table with mapped fields, match status (link to existing service), verbatim originals in a details toggle, per-row Import/Discard with decided-by input, outcome summaries; native `.xlsx` uploads are rejected with a "save as CSV first" message. `GET /api/services/export` downloads the canonical directory (`lous-place-services-<date>.csv`, always canonical, never staging). `docs/synthetic-lou-service-list.csv` extended to a full 5-service demo spreadsheet (mapped + unmapped columns: Who it's for / Cost / Wait time / Notes).
- DB changes: new tables `spreadsheet_imports` (filename, imported_by, imported_at, row_count) and `staged_services` (verbatim raw_values jsonb, typed mapped columns, match_status new/matched, matched_service_id plain reference, status staged/imported/discarded, outcome jsonb, decided_by/decided_at) via drizzle-kit push, with checks + index. No changes to `services` / `service_attributes` structure (import only inserts/fills).
- Tests run: new `npm run db:test:spreadsheet` — **33/33** (parser edge cases; header/need alias mapping incl. unknown columns preserved; staging matches by normalised name; import: empty field filled, non-empty kept, missing need added without duplicating, excel provenance + needs_provider_confirmation on added facts; new-row import creates the canonical service; decided rows rejected by both actions; discard once-only, canonical untouched; export: header row, kept-vs-filled values, imported service with needs; cleanup). Regressions: `db:test:admin` 16/16.
- Result: pass — upload → review → import/discard flow works end to end through the admin forms; canonical values are never overwritten; export reflects canonical data.
- Known issues: needs column uses `;`, `,` or `/` separators (per-cell splitting); only one spreadsheet batch format tested in the demo — aliases cover common Lou-style headers but new spreadsheets may need alias additions.
- Next phase: awaiting user manual test of Phase 8; manual test steps below.

Manual test steps (Phase 8):
1. Admin → "Spreadsheet migration (staging)" → choose `docs/synthetic-lou-service-list.csv`, enter your name → Upload & stage. Expect: green summary "Staged 5 rows …: N new, M matched" and 5 staged rows (nothing canonical changed).
2. Expand a row's "verbatim" details — original cells (incl. Who it's for / Cost / Wait time / Notes) are preserved exactly.
3. Import a **matched** row (e.g. Bright Path): expect "empty field(s) filled / need(s) added / existing value(s) kept" — check the service page: existing values (e.g. phone) unchanged, empty ones filled, new needs show `excel import` + `needs provider confirmation`.
4. Import a **new** row: expect a new service appears in the admin list with `excel import` source and its needs flagged for provider confirmation.
5. Discard another staged row: expect "Row discarded — canonical data untouched"; try importing it again → error "already decided".
6. "Download canonical directory (CSV)" → opens in Excel with the imported changes reflected.
7. Try uploading a native `.xlsx` → expect the "save it as CSV first" error message.

## 2026-09-05 — Phase 7B complete: discovery review/merge + scheduling
- Branch: `project-status-overview`
- Changes: the human half of new-service discovery. `src/lib/discovery.ts` gained `approveDiscoveryCandidate` / `rejectDiscoveryCandidate`: **approve** creates the canonical service in one transaction from the candidate's stored evidence — extracted service fields → `services` columns (`source_type='discovery_review'`, status active), attribute facts → `service_attributes` rows with machine provenance and the candidate's retrieval time — then appends the decision to the service change log ("Discovery approved by …" with candidate, source URL, evidence type, retrieval date); **reject** records `decided_by`/`decided_at` only — canonical data untouched; only `pending_review` candidates are decidable, and decided rows are never re-queued (queue dedupe covers any status; a merged URL becomes a known canonical source URL). Admin queue UI: per-row decided-by input + Approve / Reject (formAction) buttons, decided rows show who/when; server actions `approveDiscoveryAction` / `rejectDiscoveryAction` (errors via `discoveryError` param). `POST /api/discovery/run` now echoes `trigger: manual|scheduled`; new `discovery-cron` compose sidecar POSTs the scheduled endpoint every 6h (Base44's scheduler can target the same URL once the app is published) — discovery is manually triggerable for the demo without waiting for the cron. Also fixed the discovery test suite's DB scope: test rows are now matched on `*.example.org` URLs only — the old `https://%` pattern wiped real queued candidates when the suite ran against the live DB.
- DB changes: `discovery_candidates` gained nullable `decided_by` (text) + `decided_at` (timestamptz) via drizzle-kit push. No changes to `services` / `service_attributes` structure (approval only inserts rows).
- Tests run: `npm run db:test:discovery` extended 10 → **17/17** (approval creates the canonical service with extracted fields + discovery provenance; attribute facts inserted with machine provenance + the candidate's retrieval timestamp; candidate marked merged with who/when; already-decided candidates rejected safely by both actions; append-only change log records the discovery approval; rejection records the decision with services/attributes counts unchanged; repeated run queues nothing with merged/rejected/pending candidates). Regressions all green: `db:test:brightdata` 14/14, `db:test:updater` 19/19, `db:test:admin` 16/16, `db:test:verify` 15/15. **Live against the real Bright Data API:** manual discovery run queued 8 real candidates; "Women's Housing Company" approved through the real admin form → canonical service created (phone 1300 942 111, `need=housing_accommodation` fact, `discovery_review` provenance, change-log row, candidate merged by Lou (admin)); NSW-government info page rejected through the form → canonical service count unchanged, candidate rejected; re-run with the same queries: 0 created, 8 skipped; scheduled trigger verified from the cron sidecar (`trigger: "scheduled"` response).
- Result: pass — all acceptance criteria verified live: scheduled/manual run calls the Bright Data API, duplicates detected, a genuinely new service appeared in the queue with provenance, approval created canonical records, rejection left canonical data unchanged, repeated runs duplicated nothing.
- Known issues: the demo DB now holds one merged candidate ("Women's Housing Company" — real data, machine-extracted; provider confirmations can be added via the Verify stage), one rejected candidate (NSW.gov info page), and 6 pending real candidates left for manual review; `BRIGHT_DATA_UNLOCKER_ZONE` still not set — pages that block direct fetching are logged as source failures until it is added.
- Next phase: none requested — the new-service-discovery process (queueing, review/merge, scheduling) is complete; docs updated (`implementation_plan.md` data flow #7, `build_plan.md` phase 7 status).

## 2026-09-05 — Bright Data re-architecture: SERP API + Web Unlocker (Phase 7A updater + 7B discovery pipeline)
- Branch: `project-status-overview`
- Changes: replaced the Web Scraper Dataset API integration (and its `BRIGHT_DATA_DATASET_ID` dependency) with two small replaceable adapters in `src/lib/brightdata.ts`, both over Bright Data's unified `POST api.brightdata.com/request` endpoint with Bearer auth and injectable fetch (unit-testable offline, provider swappable later): **SERP API** (`serpSearch()` — builds the google search URL from the query with `gl=au&hl=en`, requests `data_format: "parsed_light"`, returns parsed organic results; zone from `BRIGHT_DATA_SERP_ZONE`) and **Web Unlocker API** (`unlockerFetch()` — `{zone: BRIGHT_DATA_UNLOCKER_ZONE, url, format: "raw", data_format: "markdown"}` → `{status_code, body}` markdown). Updater flow (`src/lib/sources.ts` rewritten): direct fetch first for plain `https://` source URLs; when the fetch itself fails (network/non-200) → Web Unlocker fallback; content normalised one way — normalised JSON payloads as-is, anything else (HTML/Unlocker markdown) via new `src/lib/page-extraction.ts` LLM normalisation into the same `SourceFact[]` shape (shared primitives in `src/lib/source-facts.ts`; prompt forbids invention; output validated by the shared normaliser; same LLM env as worker stages). `brightdata:` prefix routing removed with the Dataset API. Discovery pipeline (rest of 7B review/merge NOT built): new `src/lib/discovery.ts` `runDiscovery()` — SERP queries (needs-taxonomy defaults) → provider URLs (social junk filtered) → direct fetch/Web Unlocker → normalise → dedupe (already-known `services.source_url`, already-queued candidate URLs any status, same name @ same site) → `discovery_candidates` queue rows with source URL, SERP source name, retrieval timestamp, evidence type (`direct_fetch`/`web_unlocker`) — canonical services/facts never touched. `POST /api/discovery/run` endpoint + Admin "Run discovery now" button + Retrieved/Evidence provenance columns on the queue.
- DB changes: `update_candidates_evidence_type_check` extended to `('fixture','direct_fetch','web_unlocker')` (manual ALTER — drizzle-kit does not diff checks); `discovery_candidates` gained non-canonical provenance columns `retrieved_at` + `evidence_type` via drizzle-kit push. No changes to `services` / `service_attributes` / caseworker workflow.
- Tests run: new `npm run db:test:brightdata` — 14/14 (both adapters independently, stubbed HTTP: unlocker request shape/Bearer auth/zone/markdown format, response parsing, empty-body + HTTP-error + not-configured errors; SERP google-URL construction/zone/parsed_light, organic parsing with junk entries dropped, no-results + not-configured errors; updater flow: direct fetch used as-is for JSON payloads (no unlocker/LLM call), failed direct fetch → Web Unlocker + LLM normalisation with `web_unlocker` evidence, combined error when both fail, clear direct-failure error when fallback not configured, fixture adapter untouched). New `npm run db:test:discovery` — 10/10 (SERP→URL→snapshot→queue with full provenance on rows; known-service/social/duplicate-provider dedupes with logged reasons; unreachable page → SOURCE FAILED, run completes; repeated run idempotent (0 created, no duplicate rows); failed search logged, no rows created; canonical `services`/`service_attributes` counts unchanged). Regressions all green: `db:test:updater` 19/19, `db:test:admin` 16/16, `db:test:verify` 15/15. Live: updater fixture run clean (5 services, 2 ok, 0 failed, idempotent); discovery endpoint without Bright Data env returns completed summary with clean "SERP API not configured" per-query failures; admin page renders the new button + provenance columns; leftover rows from a mid-verification test crash were cleaned from the demo DB.
- Result: pass (adapters verified offline against Bright Data's documented request/response shapes; live verification against the real Bright Data service pending the user adding the env keys)
- Known issues: `BRIGHT_DATA_API_KEY`, `BRIGHT_DATA_SERP_ZONE`, `BRIGHT_DATA_UNLOCKER_ZONE` not yet set in Base44 — updater falls back cleanly (direct fetch or logged failure), discovery logs "not configured" per query; SERP/Unlocker request shapes match Bright Data's current docs but have not been exercised against the live API; discovery review/merge actions (rest of 7B) intentionally NOT built
- Next phase: rest of Phase 7B (discovery review/merge into canonical services) — NOT started; do not build until requested

## 2026-09-05 — Phase 7A: Existing-service updater
- Branch: `project-status-overview`
- Changes: scheduled + manual existing-service updater that keeps known services current from machine-accessible sources. `src/lib/sources.ts` — source adapters normalising everything into one snapshot shape (`facts[]` + source name/URL + evidence type + retrieval time): fixture adapter (deterministic demo snapshots keyed by `services.source_url`, per plan decision #2 — Southside unchanged, Bright Path phone changed + new wait-time fact), direct-fetch adapter (plain HTTP GET of a page serving the normalised payload), and a Bright Data Web Scraper API adapter called directly over HTTP (POST `/datasets/v3/scrape?dataset_id=…`, long jobs fall back to trigger→poll `/progress/{id}`→download `/snapshot/{id}`; NO CLI dependency; requires optional `BRIGHT_DATA_API_KEY`/`BRIGHT_DATA_DATASET_ID` env — user declined to set them, so the adapter reports a clean "not configured" source failure; fixtures/direct paths work without it). `src/lib/updater.ts` — `runUpdater({trigger, only?})`: per active service → fetch snapshot → compare with canonical facts (multi-value aware: same value → refresh that row; single stored row → change candidate; multi-row key with unknown value → "additional value" candidate; no stored row → "new fact" candidate; unchanged values refresh freshness in place — stale → verified_machine, provider-confirmed/admin-corrected keep human status) → every change becomes an `update_candidates` row with full provenance (source URL, evidence type, retrieval time, reason); NOTHING auto-applies (build plan rule). `applyUpdateCandidate` (admin approve): service field or fact updated in place with machine provenance + append-only `service_change_log` row ("Updater approved by …", old → new); new-fact approvals insert the fact; `rejectUpdateCandidate`: canonical untouched, rejection recorded with who/when/why. Idempotency: same-value pending candidates dedupe; changed evidence updates the pending candidate in place (newest evidence wins); rejected values are not re-proposed from the same source. `POST /api/updater/run` (manual + scheduled) and Admin "Run updater now" button + pending-candidate approve/reject forms + recent-runs table with expandable per-source run logs. `updater-cron` compose sidecar hits the endpoint hourly (first run ~3 min after boot — it already fired once as `scheduled` during verification).
- DB changes: new `updater_runs` (trigger/status checks, counts, structured jsonb log) and `update_candidates` (scope/evidence/status checks, provenance columns, decided_by/decided_at, service+scope+key+status index) via drizzle-kit push. No seed changes.
- Tests run: new `npm run db:test:updater` — 19/19 passed (run creates candidates for changed/new facts with provenance, canonical untouched by the run; unchanged stale fact refreshed stale→verified_machine; provider-confirmed fact keeps human status on refresh; new fact source-evidenced not invented; repeat run idempotent — 0 created, 4 deduped; newer evidence updates pending candidate in place; approve applies service-field + attribute + new-fact changes to canonical with change-log history; approve/reject on already-decided candidates rejected safely; reject leaves canonical value unchanged; unreachable source → run completes with sources_failed=1 + logged failure + zero candidates; post-decision re-run creates no duplicates). Regressions: `db:test:admin` 16/16, `db:test:verify` 15/15; `db:test:matching` has only the known pre-existing seed-state assertion failure (demo pets provider-confirmation), unrelated. Live verification: `curl POST /api/updater/run` created exactly 2 candidates (Bright Path phone + wait-time new fact), 8 freshness refreshes, 3 no-source services skipped with logged reasons; second run idempotent; the compose cron sidecar fired a `scheduled` run on its own; admin UI approve of the phone candidate applied it (services row = new phone, change-log row "Updater approved by Lou (admin)", candidate applied with decided_by, pending count 2→1); wait_time candidate left pending for the manual demo. A live multi-value bug was found and fixed during verification (Southside's two stored `need` rows produced a phantom candidate — now value-set aware).
- Result: pass
- Known issues: Bright Data adapter implemented per their documented HTTP API but never exercised against the real service (no API key set) — will report a source failure until `BRIGHT_DATA_API_KEY`/`BRIGHT_DATA_DATASET_ID` env vars are provided; the demo `wait_time` candidate for Bright Path is intentionally left pending for the user's manual review test
- Next phase: Phase 7B new-service discovery — NOT started; do not build until requested

## 2026-09-05 — Phase 7 (admin side): Service-knowledge view
- Branch: `project-status-overview`
- Changes: minimal admin/service-knowledge view, no analytics/dashboards. `src/lib/admin.ts` (overview query with per-service fact aggregates + freshness; detail query; append-only history query; discovery-queue query; `updateServiceAdmin` — only actually-changed core fields update+log, `updated_at` refreshed, no-op patches log nothing; `correctServiceAttribute` — updates the existing fact row in place with `source_type=manual`, `source_name='Admin correction'`, fresh `retrieved_at`, new `verification_status='admin_corrected'`, and logs value/source/status/notes changes so the ORIGINAL provenance stays inspectable in history); routes `/admin` (service list: status, source, provider-confirmed count, update candidates = stale/needs-confirmation/unknown facts, last-checked date; discovery-candidates queue section, inspect-only until 7B) and `/admin/services/[id]` (core-field edit form, structured-facts list with per-fact source/provenance/freshness/provider-confirmation + inline correct form, change-history table newest first) with server actions `saveService`/`correctFact` (same redirect-error pattern as worker stages, `changedBy` required); nav link added. Matching (`FRESHNESS_RANK`) ranks `admin_corrected` with `provider_confirmed`; Verify's `isKnownFact` treats it as known — corrected facts feed caseworker queries immediately and are never sent back to provider confirmation.
- DB changes: new `service_change_log` table (append-only, service cascade, entity check, service+created_at index) via drizzle-kit push; `service_attributes_verification_status_check` extended with `admin_corrected` (manual ALTER — drizzle-kit does not diff check constraints)
- Tests run: new `npm run db:test:admin` — 16/16 passed (service correction persists + logs old→new with who, no-op patches log nothing; fact correction updates in place with admin provenance + fresh timestamp, no parallel duplicate, original provenance preserved in the log; CASeworkER QUERY ACCEPTANCE: after correcting pets 'welcome'→'not_allowed' the match hard-excludes the service, after correcting back it is suitable again, admin-corrected facts count as known in Verify grouping; history append-only stacking; overview aggregates; guards: unknown service/fact ids + empty value rejected; cleanup cascades the log); regressions `db:test:verify` 15/15, and the only 2 failures across `db:test`/`db:test:matching` are the pre-existing known seed-state assertions (demo run provider-confirmed the seeded Watershed pets fact) — unrelated to this change. Live preview verified: `/admin` lists all 5 services with provenance/freshness/update-candidate counts; `/admin/services/[id]` shows facts with source/freshness/provider confirmations, edit form, and change history; 0 console errors, 0 failed requests; caseworker case page still 200. Form submission through the live UI was NOT exercised (preview tool budget ran out) — the actions are the same verified pattern and the underlying lib calls are 16/16 tested; included in manual test steps.
- Result: pass with notes — admin can inspect where a fact came from, edit/correct services and facts, corrections persist with history, and caseworker queries use corrected data (proven by automated tests; live form click-through left for the manual test)
- Known issues: none for the admin side; processes A (updater) and B (discovery + candidate review/merge) NOT started
- Next phase: Phase 7A/7B service-data processes — not started; do not build until requested



## 2026-09-05 — Phase 6b: Caseworker end-to-end integration & hardening

- Branch: `project-status-overview`
- Changes: **Fixed persistence bug** — `contextFromFormData` (Context draft save) returned early, leaving the Phase 5 `field_sources` tagging as unreachable dead code: every worker edit of a draft context silently dropped the woman-stated/worker-observation tags (Refer then fell back to legacy defaults). Extracted the pure mapping into `src/lib/context-form.ts` (testable, tags now kept on save); `actions.ts` imports it. **Fixed duplicate data** — `generateReferralDraft` allowed unlimited drafts per case+service (how the demo case accumulated 3 parallel Watershed referrals): added `findActiveReferralForService` guard in `src/lib/refer.ts` + `refer-actions.ts` — a new draft for a service is rejected while a draft/sent/responded referral exists (clear error; new referral allowed once the old one is closed by a final outcome). **Fixed broken state-transition silence** — `saveReferralDraft` (empty text) and `markSent` (non-draft referral) were silent no-ops; both now surface `referError` messages. No UI redesign, no schema changes.
- DB changes: none to schema/migrations. Demo data RESET to a clean scenario: deleted the case's `case_documents`/`referrals` (cascaded `referral_events`)/`case_contexts` (kept the case + original notes; service data was already at seed state incl. the Watershed pets fact), then ran the ENTIRE workflow on CASE-2026-001 (see `docs/demo_walkthrough.md`): extraction → tag corrections → approve (Context v1) → Find support (Watershed 2 needs, pets needs-confirmation) → pets provider confirmation ("welcome", Caseworker — phone) → referral draft (live LLM) → worker edit → duplicate-guard attempt (blocked live) → mark sent (due 2026-09-12) → provider response → follow-up draft (live LLM) → outcome support_received (closed) → case-note draft (live LLM, six sections) → worker edit → approved. Final demo state: 1 case / 1 approved context with tags / 1 closed referral (support_received) / 3 referral_events / 1 approved case note.
- Tests run: `db:test:refer` 23/23 (new: duplicate guard — draft/sent active, closed frees the service, other services unaffected); `db:test:context` 29/29 (new: form mapping keeps worker-corrected field_sources, untagged → woman-stated, valueless fields untagged, DB round-trip); regressions `db:test` 21/21, `db:test:matching` 24/24, `db:test:verify` 15/15, `db:test:followup` 19/19, `db:test:document` 21/21. Live UI-verified in the preview: context extraction/save-with-tag-corrections/approve (tags confirmed in DB), Verify pets confirmation (row updated in place, no duplicate), Refer draft generation + worker edit + duplicate guard error. Stages 4b–6 (mark-sent, response, follow-up draft, outcome, case-note draft/edit/approve) executed through the same lib functions the server actions call (live LLM), then verified by server-rendered page curl (closed status, outcome banner, timeline, approved note all present) — the form wiring for those steps was live-verified in Phases 5A/5B and unchanged except error surfacing. Web/DB logs during the run: zero new errors (only the intentional duplicate-guard redirects).
- Result: pass with notes — full workflow proven end to end on one clean scenario; one cosmetic note: LLM drafts store Windows-style line endings (renders fine; normalize in a future polish pass if desired)
- Known issues: the preview tool budget ran out before mark-sent/follow-up/document could be re-clicked in the live UI this session — they are function-level verified here and UI-verified in earlier phases; include them in the manual test
- Next phase: Phase 7 service-data processes (admin) NOT started — do not build until requested

## 2026-09-05 — Phase 6 step 5B: Documentation (case note drafting, review, approval)

- Branch: `project-status-overview`
- Changes: `src/lib/document.ts` (`getCaseDocuments` newest first; `getProviderConfirmationsForCase` — provider-confirmed service_attributes for the case's referred services only, with who/when; pure `buildCaseNoteInput` — stored data only: original notes verbatim, appointment date, approved context split into woman-stated vs worker-observed via stored field tags, referrals (service, status, sent date, outcome + outcome notes, follow-up due, sent text), provider confirmations, follow-up activity = provider responses + outcomes only (follow-up drafts excluded); `draftCaseNoteText` LLM instructed to use ONLY provided data with exact sections Woman said / Current concerns / Actions taken / Referrals / Worker observations / Next steps, woman's own words never containing caseworker observations; `insertDocumentDraft` / `saveDocumentDraftText` (draft-only guard) / `approveDocument` (draft-only guard, stamps approved_at); approved notes are read-only, never edited or re-approved); `src/app/women/[id]/document-actions.ts` (server actions draftDocument / saveDocumentDraft / approveDocumentAction, errors via `documentError` param, nothing persisted on failure); `DocumentStage.tsx` replaces the stage-6 stub: draft button, original appointment notes shown unchanged beside every draft, editable draft textarea + save, approve-as-final, approved notes read-only, multiple documents listed newest first; `page.tsx` stage 6 wired; `getReferralsForCase` extended with `outcome_notes`
- DB changes: none (case_documents table existed since Phase 1; no migrations, no seed changes)
- Tests run: new `npm run db:test:document` — 21/21 passed (pure: original notes verbatim, woman-stated vs worker-observed split via stored tags, referrals carry only stored data incl. outcome + notes, confirmations with who/when, follow-up drafts excluded from activity, null context tolerated; DB flow: draft persisted, empty text rejected, worker edits persist, approval stamps status+approved_at, guards reject edits/re-approval on approved + unknown ids, multiple documents newest first, original notes unchanged by all operations, case delete cascades documents); regressions `db:test:refer` 19/19, `db:test:followup` 19/19; live preview verified: "Draft case note" produced an LLM note with all six sections referencing only stored data (woman's own words from original notes, Watershed referral, provider response, outcome); real edit + save round-tripped to DB; approve-as-final made the note read-only approved; full page reopen confirmed approved note + original notes persist; 0 console errors
- Result: pass
- Known issues: the demo case now has one approved case note (ending with a worker-edit verification marker) — the demo's documentation data; drafting a new note any time remains possible
- Next phase: Phase 7 service-data processes NOT started

## 2026-09-05 — Phase 6 step 5A: Follow up (responses, outcomes, timeline, due follow-ups)

- Branch: `project-status-overview`
- Changes: `src/lib/followup.ts` (OUTCOMES with awaiting_reply the only non-final one — accepted/declined/referred_elsewhere/support_received/other close the referral; `recordProviderResponse` — open referrals only, moves sent → responded, appends a timeline event; `recordOutcome` — persists outcome + notes + outcome_at, awaiting_reply stays open, finals close; `getReferralEvents` / `getReferralEventsForCase` chronological timeline queries; `getDueFollowUps` — open referrals with follow_up_due <= today, for My Work; `buildFollowUpDraftInput` pure — only stored referral data enters: service, case ref, sent date, sent referral text, status, outcome label, recorded provider responses only; `draftFollowUpText` LLM instructed to use only provided data; `storeFollowUpDraft` — open referrals only, draft stored as a timeline event for worker review, never transmitted); `src/app/women/[id]/followup-actions.ts` (server actions: recordResponse, recordOutcomeAction, draftFollowUp — errors via `followUpError` param, nothing persisted on failure); `FollowUpStage.tsx` replaces the stage-5 stub: per sent referral — status + outcome pills, next follow-up date, timeline (sent date + events with dates), record-provider-response form, record-outcome form (6 options, "support received" called out as distinct from merely sent/accepted), draft-follow-up button + latest draft shown for review/copy with "nothing is sent automatically"; closed referrals read-only; `getReferralsForCase` extended with `outcome_at`; My Work (`src/app/page.tsx`) gained a "Follow-ups due" section above My cases, linking to the case with service name, due date, status, outcome; document drafting split into its own "6. Document" stub section (step 5B, not started)
- DB changes: new `referral_events` table (referral_id cascade fk, kind check provider_response/outcome/follow_up_draft, note, occurred_at, index on referral_id+occurred_at); `referrals_outcome_check` expanded to awaiting_reply/accepted/declined/referred_elsewhere/support_received/other. drizzle-kit push created the table but did NOT recreate the altered check constraint — altered manually with ALTER TABLE to match schema.ts (no data existed, outcomes were all null)
- Tests run: new `npm run db:test:followup` — 19/19 passed (pure: requested outcome options exist incl. referred_elsewhere + support_received, invalid rejected, awaiting_reply only non-final, support_received label distinct from accepted, referralIsOpen semantics, draft input carries only stored data and only provider responses — drafts/outcome events excluded; DB flow: My Work lists today-due referral but not future-dated/draft ones, guards reject responses+outcomes on draft referrals, response saved sent → responded + timeline event, second response allowed, follow-up draft stored as event, awaiting_reply persists outcome and stays open + still listed in My Work, support_received persists outcome+notes+outcome_at, closes referral, timelines the outcome, leaves the due list, closed referral accepts no further responses/outcomes/drafts, case-level timeline chronological, cleanup with no orphan events); regressions `db:test` 21/21, `db:test:context` 24/24, `db:test:matching` 24/24, `db:test:verify` 15/15, `db:test:refer` 19/19; live preview verified: stage 5 shows the sent Watershed referral with timeline, six outcome options, draft button; real form submission of a provider response → status responded + timeline event (DB row confirmed); "Draft follow-up message" produced an LLM draft referencing only stored data (service, CASE-2026-001, sent date, what was sought) shown for review; marked the second referral sent with follow-up due today → My Work "Follow-ups due" lists it (due 2026-09-05, sent) and excludes the future-dated one; 0 console errors
- Result: pass with notes — drizzle-kit push not recreating altered check constraints is a known tool limitation: schema constraint changes need manual ALTER (or drop/recreate) alongside push; pre-existing `tsc --noEmit` errors (~23) remain in VerifyStage/actions/refer-actions and the same redirect()-narrowing pattern appears in the new followup-actions — dev runtime unaffected (tsx/next dev do not typecheck); first test run crashed on the old check constraint before cleanup could run, leaving orphan test rows — removed manually, matching suite re-verified 24/24
- Known issues: the demo case now has two sent referrals from live verification (Watershed — responded, follow-up due 2026-09-12; second — sent, follow-up due today) plus one recorded provider response and one stored follow-up draft: this is the demo's follow-up data; outcomes are left unrecorded for the user's manual test
- Next phase: step 5B — document (case documentation drafting) NOT started; Phase 7 service-data processes NOT started

## 2026-09-05 — Phase 5: Refer (draft from approved context → worker review → mark sent)

- Branch: `project-status-overview`
- Changes: `src/lib/context-fields.ts` (shared field metadata: CONTEXT_FIELDS list, minimal core set needs/suburb/languages/summary, `fieldSourceOf` with legacy defaults — everything woman-stated, urgency the caseworker's assessment, `fieldValuePreview`/`fieldHasValue`); Phase 2 extraction extended (`src/lib/extraction.ts`): prompt asks for per-field woman_stated/worker_observation tags, `parseExtraction` normalises them (invalid → woman_stated, only fields with values); Context draft review (`ContextStage.tsx` + `actions.ts`) gained per-field tag selects so the worker can correct tags; `src/lib/refer.ts` (pure `buildReferralDraftInput` — ONLY the checked approved-context fields with values enter the draft input, excluded fields absent entirely; `draftReferralText` LLM call instructed to use only provided data with separate "About the woman (as she stated)" / "Caseworker observations" sections; `defaultFollowUpDate` = one week, worker-editable; `insertReferralDraft` / `saveReferralDraftText` / `markReferralSent` with draft-only guards — sent referrals are never edited or re-marked; `getReferralsForCase`); `src/app/women/[id]/refer-actions.ts` (server actions: generate validates service ∈ Find-support suitable results + approved context exists, LLM failure → error param, nothing persisted; mark-sent is DB-only — nothing is ever transmitted); `ReferStage.tsx` replaces the stage-4 stub (service picker from suitable results, share checkboxes grouped woman-stated / worker observations with minimal set pre-checked, editable draft, mark-as-sent with pre-filled follow-up date, sent referrals read-only); one-time backfill `src/db/backfill-field-sources.ts` tagged pre-Phase-5 contexts (1 context in this environment)
- DB changes: `referrals.shared_fields` jsonb column added (drizzle-kit push); `CaseContext.field_sources` map added to the context jsonb shape (no column change — jsonb); existing contexts backfilled via `npm run db:backfill-sources`
- Tests run: `npm run db:test:refer` — 19/19 passed (pure: minimal core set only, excluded fields appear nowhere in the draft input — visa/children/pets/income/urgency/safety/contact all absent, woman-stated vs worker-observation grouping, opt-in sharing works, nothing-recorded fields dropped, legacy defaults (urgency → worker observation), stored tags respected, parseExtraction tag normalisation, follow-up default = one week; DB flow: draft persisted with shared fields + approved context + service link, worker edit saved, mark sent persists status + sent_at + follow_up_due, fresh query preserves everything, sent referrals never change — edit and re-mark are no-ops, cleanup runs twice cleanly); regressions `db:test` 21/21, `db:test:context` 24/24, `db:test:matching` 24/24, `db:test:verify` 15/15; live preview verified: context v1 approved → Refer stage shows 3 suitable services, minimal set pre-checked with urgency under worker observations → "Generate referral draft" produced a factual draft from shared fields only with the woman-stated section → worker edited the draft (removed the visa mention carried inside the shared summary text) and saved → "Mark as sent" with follow-up 2026-09-12 (one week default) → Postgres row: status sent, shared_fields needs/suburb/languages/summary, follow_up_due 2026-09-12, sent_at set, visa excluded; 0 console errors
- Result: pass with notes — the browser-level reload check of the sent referral was not re-exercised (preview tool budget), but reload persistence is proven by the DB suite (fresh-query assertions) and the page renders state from a fresh server query on every load; note the seeded summary text itself mentions children/dog/visa — sharing the summary shares whatever the summary says, so the worker reviews the draft (demonstrated live by removing the visa mention)
- Known issues: none outstanding for Phase 5; a full browser reload walkthrough is included in the manual test steps below
- Next phase: awaiting user manual test of Phase 5; then Phase 6 — Follow up + document — NOT started

## 2026-09-05 — Phase 4: Verify (provider-only unknowns)

- Branch: `setup-branching-rules`
- Changes: `src/lib/verify.ts` (pure `groupFacts`: machine-known vs genuinely-provider-only unknowns incl. missing facts for context-relevant criteria, stale facts with kept history; `recordProviderConfirmation` updates the EXISTING fact row in place — or inserts the missing one — with source_type provider_confirmed, who/when, notes, retrieved_at; `markFactStale` flips only verification_status, never deleting history; `getServiceForVerify`); `src/app/women/[id]/verify-actions.ts` (server actions with validation errors surfaced via `verifyError` param); `VerifyStage.tsx` replaces the stage-3 stub: service picker limited to Find-support-suitable services, "Already known from machine-accessible sources" group (source + freshness per fact, mark-stale control), "Needs provider confirmation" group (unknown values, stale facts with visible history, missing facts as explicit unknowns — never assumed), one inline confirmation form per item (value / confirmed by / when / notes). No LLM, no machine refetching (Phase 7A's job); worker never re-checks digital facts manually
- DB changes: none (no schema changes; provider confirmations update existing service_attributes rows — shared knowledge, never parallel duplicates)
- Tests run: `npm run db:test:verify` — 15/15 passed (pure grouping: unknowns never claimed known, missing-criteria unknowns, stale history kept, no duplicates across groups, irrelevant criteria produce no phantom unknowns; DB flow: confirmation updates existing row with full provenance, no duplicate fact, fresh query reuses it like another case, Find-support match reflects the confirmation, missing-fact insert, mark-stale keeps source/who/when/notes and returns the fact to the needs list, cleanup); regressions `db:test` 21/21, `db:test:context` 24/24, `db:test:matching` 24/24; live preview verified: Watershed selected from Find-support results → 3 known / 6 needs-confirmation; real form submission of a pet-policy confirmation → persisted in DB (value/welcome, provider_confirmed, Caseworker — phone, today, notes), UI moved it to the known group, and Find support immediately showed "pets: matched — Caseworker confirmed 0 day(s) ago"; 0 console errors
- Result: pass with notes — mark-stale button verified by automated tests (lib-level update + grouping history) but its live UI click not exercised before the preview budget ran out; left for the user manual test
- Known issues: the seeded Watershed pets fact is now a provider-confirmed "welcome" row (written by the live verification, by design — shared knowledge); user can mark it stale to demo expiry and re-confirm
- Next phase: awaiting user manual test of Phase 4; then Phase 5 — Refer — NOT started

## 2026-09-05 — Phase 3: Find support (deterministic matching from approved context)

- Branch: `setup-branching-rules`
- Changes: `src/lib/matching.ts` (pure `evaluateService`/`matchServices` over typed `service_attributes` rows + `getLatestApprovedContext` / `getMatchCandidates` query helpers); `src/app/women/[id]/FindSupportStage.tsx` (server component: per-criterion evidence, source + freshness per fact, explicit unknowns — "not recorded" vs "needs provider confirmation", "Not suitable" collapsed section with one-line deterministic reasons); case workspace stage 2 now renders real results instead of the stub. No LLM anywhere in the match path; ranking is deterministic (matched needs, then freshness provider_confirmed > verified_machine > stale, then name) and no confidence score exists in the code or UI. Matching only ever runs against the latest APPROVED context; drafts never used. Hard exclusions (children/pets/visa restrictions from stored values) exclude a service with a reason; language/income mismatches flag but don't exclude (choosing is the worker's call)
- DB changes: none (no schema changes; one test-only fix: `db:test` "jsonb context query finds the case by need" now asserts the seeded case is found rather than an exact row count — the Phase 2 walkthrough legitimately added context versions to the seeded case)
- Tests run: `npm run db:test:matching` — 24/24 passed (pure matching on seed-mirroring fixtures: needs match with provenance, pets unknown flagged, stale wait flagged, children restriction excludes with stored-value reason, language mismatch flagged not excluded, deterministic ranking, context change flips results; DB flow: drafts never used despite higher-version draft, seeded services match Watershed/Southside/Bright Path and exclude New Dawn/Harbour with reasons, re-approved context recomputes results, every displayed fact carries source + freshness, cleanup); `db:test` 21/21 and `db:test:context` 24/24 re-run green; live preview verified against the user's own manually-approved Context v3 — Watershed suitable with evidence + unknowns, 4 services excluded with reasons, 0 console errors
- Result: pass — all Phase 3 acceptance criteria verified (LLM-free path, evidence + freshness per criterion, unknowns explicit, results change with approved context, no fake confidence)
- Known issues: none outstanding for Phase 3
- Next phase: awaiting user manual test of Phase 3; then Phase 4 — Verify — NOT started

## 2026-09-05 — Phase 2 fix: Server Actions origin guard (correct config path) + full browser verification

- Branch: `setup-branching-rules`
- Changes: the earlier origin fix was in the wrong config location — `experimental.allowedOrigins` is ignored by the action CSRF check; the correct path is `experimental.serverActions.allowedOrigins` (read from `node_modules/next/dist/server/app-render/action-handler.js` + `csrf-protection.js`: single `*` matches exactly one subdomain label, so both `*.base44-preview.app` and `*.imported.base44-preview.app` are listed)
- DB changes: none
- Tests run: full live-preview walkthrough — rough notes entered → "Extract draft context" → **Draft v2 — not approved (extracted by google/gemini-2.5-flash)** rendered editable (Ashfield / housing_accommodation / 1 child / high / email / student visa all correctly extracted); worker edited suburb and saved (persisted); clicked Approve; hard reload shows **Context v2 approved** with timestamp; Postgres shows v2 approved with the edited value and the seeded v1 draft untouched; 0 console errors; `db:test` and `db:test:context` suites remain green (21/21, 24/24)
- Result: pass — all Phase 2 acceptance criteria verified in the browser and DB
- Known issues: none outstanding for Phase 2
- Next phase: awaiting user manual test of Phase 2; then Phase 3 — Find support

## 2026-09-05 — Phase 2: Context (notes → draft → review → approve)

- Branch: `setup-branching-rules`
- Changes: Context stage built out in the case workspace (`src/app/women/[id]/ContextStage.tsx` + server actions `src/app/women/[id]/actions.ts`): notes form saves raw notes on the case and runs LLM extraction (`src/lib/extraction.ts` — OpenAI-compatible chat call via LLM_BASE_URL/LLM_API_KEY/LLM_MODEL, `parseExtraction` pure normaliser mapping to the CaseContext jsonb shape with needs-taxonomy tokens); extraction ALWAYS inserts a new draft version (max+1) with extraction_model provenance; draft review form is editable and clearly marked "Draft — not approved"; Save changes updates only the draft row; Approve flips draft → approved with approved_at (draft-only guard — approved rows are never modified); approved state renders read-only; extraction failures show a page error and insert nothing. Also: `next.config.ts` gained `experimental.allowedOrigins` for the Base44 preview proxy (dev-mode Server Actions guard aborts action POSTs whose origin ≠ x-forwarded-host — proxy-origin mismatch, not app logic); LLM_MODEL secret corrected to OpenRouter model ID `google/gemini-2.5-flash` (was display name "Gemini 2.5 Flash" → HTTP 400); standalone latest-context table on the case page folded into the Context stage
- DB changes: none (no schema changes; existing cases.original_notes + versioned case_contexts reused as designed)
- Tests run: `npm run db:test:context` — 24/24 passed (parseExtraction normalisation incl. fenced/prose JSON, taxonomy tokens, unknown-needs kept; DB flow: notes persist → draft v1 → in-place edit → approve with timestamp → draft-only guard leaves approved row untouched → re-extract creates v2 draft, v1 intact → reload shows latest with correct status; cleanup); `npm run db:test` re-run 21/21; live LLM smoke test via `extractContextFromNotes` returned correctly structured context (model google/gemini-2.5-flash)
- Result: pass with notes — automated + live-LLM verified; browser click-through of the form flow NOT yet re-verified after the Server Actions origin fix (first two attempts were blocked by stale action bindings after a service restart, then by the origin guard itself) — left as the user manual test
- Known issues: preview proxy origin differs from sandbox host, so Server Actions in dev need `experimental.allowedOrigins` (done; delete if deploying behind a same-origin host); seeded v1 draft is a placeholder until the user runs a real extraction
- Next phase: awaiting user manual test of Phase 2 (click-through steps provided); then Phase 3 — Find support — NOT started

## 2026-09-05 — Phase 1b: Minimal caseworker shell

- Branch: `setup-branching-rules`
- Changes: sparse app shell with nav (My Work / Women / Data check); `/` = My Work (case list, no metrics dashboard); `/women` = women/case list from DB; `/women/[id]` = case workspace loading case header, original notes, latest context version, referrals, and the five workflow stages (Context → Find support → Verify → Refer → Follow up + document) as stub sections; Phase 1 verification page moved to `/data`; no workflow logic built
- DB changes: none (no schema changes)
- Tests run: all routes curl-tested — `/`, `/women`, `/data`, `/women/[seeded id]` return 200, unknown id 404s; preview click-through My Work → Women → case workspace verified (case loads with client ref, draft context pill, all five stage sections; 0 console errors)
- Result: pass — navigation, case loading, and DB-backed state proven; UI intentionally sparse
- Known issues: one dev-only fix mid-build (`women/page.tsx` import path); none outstanding
- Next phase: Phase 2 — Context (LLM extraction) — NOT started; awaiting user manual test of Phases 1/1b

## 2026-09-05 — Phase 1 fix: distinct provider-confirmed status

- Branch: `setup-branching-rules`
- Changes: added `provider_confirmed` as a distinct `service_attributes.verification_status` value (was conflated with `verified_machine`); schema check extended, seed's provider-confirmed facts (New Dawn, Harbour referral_required) now use the new status, confirmation-recording test asserts the distinct status, verification page shows distinct badges (green provider-confirmed vs blue machine-verified)
- DB changes: check constraint altered to include `provider_confirmed` (drizzle-kit push did not detect the check-constraint diff, so applied via SQL — known drizzle-kit limitation for this change type); 4 existing confirmed rows migrated `verified_machine` → `provider_confirmed`
- Tests run: full `npm run db:test` re-run — 21/21 passed
- Result: pass — page shows machine-verified (blue) and provider-confirmed (green) distinctly; 0 console errors
- Known issues: drizzle-kit push does not diff check-constraint changes; if a check is altered again, apply the `ALTER TABLE` manually
- Next phase: awaiting user manual test of Phase 1; Phase 2 not started

## 2026-09-05 — Phase 1: Database foundation

- Branch: `setup-branching-rules`
- Changes: Next.js fullstack skeleton (App Router) + Postgres 16 in `docker-compose.base44.yml`; Drizzle schema `src/db/schema.ts` (services, service_attributes, cases, case_contexts, referrals, case_documents, discovery_candidates — per approved plan, no redesign); idempotent seed `src/db/seed.ts` (5 services with mixed provenance — excel_import / machine / provider_confirmed, 20 service_attributes incl. needs_provider_confirmation + stale facts, 1 synthetic woman/case with draft context); minimal data-verification page `src/app/page.tsx`; relationship/query tests `src/db/test.ts`
- DB changes: initial schema via `drizzle-kit push` (check constraints + indexes per plan); synthetic seed
- Tests run: `npm run db:test` — 21/21 passed: create/read/update across service→attribute, case→context→referral joins, referral status progression (draft→approved→sent with sent_at), provider confirmation recording, outcome update, deterministic need query with provenance/freshness, jsonb context query, discovery dedup, cleanup with cascade check
- Result: pass — app serves on port 3000 under external preview host; preview renders seeded data with source/status badges, 0 console errors, 0 failed requests
- Known issues: seed provenance values are synthetic placeholders (real Excel/page snapshots not yet imported — by design for Phase 1); Postgres exposed on host port 5432 for external inspection (dev-only credentials)
- Next phase: Phase 2 — Context (LLM extraction) — NOT started; awaiting user manual test of Phase 1

## 2026-09-05 — Design decisions locked (pre-Phase 1)

- Branch: `setup-branching-rules`
- Changes: recorded the five design decisions in `docs/implementation_plan.md` (Drizzle ORM; real Excel/public-page data snapshot into deterministic fixtures; initial needs taxonomy; case-context fields with gender dropped; no auth / single caseworker)
- DB changes: none
- Tests run: reviewed plan for consistency (stack, context fields, matching examples)
- Result: pass
- Known issues: needs taxonomy is a placeholder until refined with Lou's actual list
- Next phase: Phase 1 — Foundation (Next.js + Postgres + Drizzle schema + snapshot-fixture seed)

## 2026-09-05 — Phase 0: Project memory

- Branch: `setup-branching-rules`
- Changes: Wrote project memory docs: product definition, phased build plan (phases 1–7, one per workflow step), implementation plan with proposed Postgres schema and data flow, and this log format.
- DB changes: none
- Tests run: verified all four docs render as valid Markdown; schema tables cross-checked against the five-step workflow and the two long-term service-data processes
- Result: pass with notes — schema, phase list, and open decisions presented to the user for review before any implementation
- Known issues: none
- Next phase: Phase 1 — Foundation (Next.js + Postgres + schema + synthetic seed) — awaiting user decisions on ORM, sources, needs taxonomy, context fields, and auth scope

## 2026-09-05 — Phase 0: Docs scaffolding

- Branch: `setup-branching-rules`
- Changes: Created `docs/` structure (product.md, build_plan.md, implementation_plan.md, implementation_logs.md) and `AGENTS.md` with the branch/session rules.
- DB changes: none
- Tests run: verified all five files exist and render as valid Markdown
- Result: pass
- Known issues: none
- Next phase: fill docs with real project content

## 2026-09-05 — Removed Web Unlocker fallback from source fetching (Phase 7)

- Why: the Bright Data Web Unlocker fallback is a production-only concern and not required for the demo; provider pages that block direct fetching should simply be recorded as source failures. The SERP API discovery path is kept unchanged.
- Branch: `project-status-overview`
- Changes: `src/lib/brightdata.ts` — removed `unlockerFetch()`/`UnlockerContent` and the `BRIGHT_DATA_UNLOCKER_ZONE` env reference (SERP adapter untouched). `src/lib/sources.ts` — `fetchSnapshot()` is now direct-fetch only: a failed fetch (network error / non-200) throws `direct fetch of <url> failed (<reason>)`, which the updater/discovery log as a source failure; removed the `unlocker` injection boundary and `unlockerDefault()`. Comments/admin copy updated; no behaviour changes to fixtures, normalisation, dedupe, queueing, or review.
- DB changes: none (the `web_unlocker` value stays in the evidence-type check constraints for historical rows; nothing produces it now)
- Secrets: `BRIGHT_DATA_UNLOCKER_ZONE` removed from `.base44/environment.json` (was never provided)
- Tests run: `npm run db:test:brightdata` (rewritten for the new flow), `npm run db:test:discovery`, `npm run db:test:updater`, plus a live manual discovery run
- Result: pass (10/10 brightdata, 17/17 discovery, 19/17 updater, live manual discovery run: SERP ok, blocked URLs logged as plain source failures, no unlocker references anywhere)
- Known issues: blocked provider sites (HTTP 403) now always end as logged source failures — expected demo behaviour
- Next phase: n/a

## 2026-09-06 — Separated the five-step journey from follow-up history

- Why: the active profile and follow-up links still opened the stale sage-green vertical workflow, and the Follow-ups page mixed case progression with referral communication history.
- Changes: every profile now shows the five referral stages in a compact warm-glass journey; the ranked plan uses the same five stages in its sticky action rail. Follow-ups is now a separate Postgres-backed history of sent emails, service replies and recorded actions, filterable by overdue state, location, children, pets, income, language and visa. My Work, People and Follow-ups link to the new history route for the selected person.
- Stale routes: /women, /women/[id] and /clients/[id]/workflow now redirect into the active A2 interface. The unused A2 vertical workflow loader and sheet component were removed.
- Visual system: active green and seafoam accents were replaced with the current white, dim-orange and dusty-blue glass palette.
- Tests run: deferred while the visual-tweak batch is active; git diff --check was used for the edited source.
- Result: implementation complete, visual browser review pending with the rest of the tweak batch.
- Known issues: the retained files under src/app/women/[id]/ are inactive legacy action/component code and have no reachable page.

## 2026-09-06 — Faster navigation and Today service activity

- Why: the navbar gave no visible response until the next server-rendered page completed, and the first development visit could spend about ten seconds compiling a route. The Today rail also showed accommodation rows instead of the background service-data work.
- Changes: the nav loop now moves immediately on click and all five dashboard routes prefetch after the shell loads. Added a prominent New call note shortcut beside the caseworker controls. Replaced Accommodation availability with Postgres-backed recent service changes and newly discovered services, including source website links.
- Demo data: the idempotent seed adds one synthetic Bright Data hours change and one discovery-reviewed NSW Domestic Violence Line record so both Today sections are visible.
- Performance evidence: People database reads completed in 16 ms. A clean Turbopack server compiled Today in 9.2 s once; warm server renders were 562–643 ms. The remaining large first-click pause was route compilation, so route prefetching now moves that work into the background.
- Verification: /today and /clients returned HTTP 200 after the clean local-only restart; the wider test suite remains deferred until the visual-tweak batch ends.

## 2026-09-06 — Amira referral demo and caseworker email settings

- Why: the demo needed one understandable path from call notes to a ranked referral, with email and phone contact choices, while the raw Services directory forced caseworkers to interpret the database themselves.
- Changes: added an optional safe client email to intake and profiles; added Hannah Lee Settings with a persistent caseworker email; made Settings reachable from Hannah's identity menu. Replaced the Shelters directory with the latest approved person's ranked top-three service plan. Service cards and the draft review show provider email, phone, From and To details, with a call banner for missing or current information.
- Mock interaction: Amira's approved structured context drives the top-three results. Synthetic Southside and Bright Path referral addresses provide demo email actions; services without email become call-only. Approve mock send records the referral in Follow-ups without claiming external delivery.
- Completion: profiles now include Close note and return to People, which records the case closed and returns to the People list.
- DB changes: added cases.client_email and the single-caseworker caseworker_settings table; the idempotent seed supplies safe example.org addresses for Amira and Hannah.
- Verification: schema push and seed completed; /settings, /shelters, /clients/new and an actual profile returned HTTP 200 with the expected controls; TypeScript passed.

## 2026-09-05 — Demo follow-up example seeder (Phase 6)

- Why: no open due referral existed, so the follow-up structure couldn't be inspected; `src/db/demo-followup.ts` (npm-style run via `npx tsx`) seeds a realistic example on CASE-2026-001 through the app's real functions only (insertReferralDraft → markReferralSent → recordProviderResponse → real-LLM draftFollowUpText → storeFollowUpDraft). Idempotent: skips if an open Bright Path referral already exists.
- DB changes: demo data only (one sent/responded referral to Bright Path Financial Counselling + timeline events)
- Tests run: live verification of dashboard due-list and case-page stage 5
- Result: pass
- Known issues: none
- Next phase: n/a

## 2026-09-06 — Wired the A2 UI to the real casework data

- Why: the A2 design screens rendered entirely from `src/lib/a2-mock.ts` with no database access. Every surface that has verifiable backend logic and data behind it is now driven by the real tables; surfaces with no backend yet (assistant runs, plans, shelter bed capacity) stay demo content and say so.
- Branch: `backend-skeleton-setup`
- Changes:
  - New view-model layer `src/lib/a2/`: `format.ts` (Sydney-timezone day/time/due labels, overdue maths, initials), `clients.ts` (client list rows + client profile), `follow-ups.ts` (due rows + waiting-on-a-service rows), `letters.ts` (case documents), `shelters.ts` (services directory with fact provenance), `today.ts` (roll-up). All read-only; they reuse the existing helpers (`getDueFollowUps`, `getReferralsForCase`, `getCaseDocuments`, `getReferralEventsForCase`, `outcomeLabel`, `factLabel`) rather than reimplementing logic.
  - Pages converted to async server components with `export const dynamic = "force-dynamic"`: `/today`, `/clients`, `/follow-ups`, `/letters`, `/shelters`.
  - `/clients/maya` (static mock profile) replaced by `/clients/[id]`, keyed on `cases.id`; the client list links to it. Profile chips/summary come from the latest context (approved preferred over a newer draft), files/timeline from note revisions, documents and referrals.
  - `/shelters`: the design's "Beds (how, when)" and "For Maya" columns are replaced by "Contact" and "Last checked" — bed capacity and per-client eligibility have no backing data. Filters are the real distinct `need` facts. The "Call list" rail was dropped (no data); "Last checked" is real attribute freshness.
  - New `src/components/a2/Empty.tsx` for in-sheet empty states.
  - Still mock, intentionally: `AskBar`, `ShelterAsk`, `Spotlight`, alerts, identity, the Today "running task" and "Shelter beds today" rail, `/plans`, `/working`, `/done`, `/states`.
- DB changes: none (read-only wiring)
- Tests run: `npx tsc --noEmit` in the web container (clean apart from stale `.next` types for the deleted route); HTTP 200 + real content checked on `/today`, `/clients`, `/clients/<id>`, `/follow-ups`, `/letters`, `/shelters`; live preview click-through from the client row to the profile verified (row `CASE-2026-001` → `/clients/b7a3d13e…`, real chips and sheets rendered, no console or network errors).
- Result: pass
- Known issues: the database currently holds one case, one draft context and five services with no referrals or documents, so `/follow-ups` and `/letters` correctly render empty states. Seeding referrals/documents would exercise those rows.
- Next phase: n/a
