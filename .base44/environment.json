{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "LLM_BASE_URL",
      "description": "Base URL of the LLM API endpoint (used from Phase 2 for context extraction and drafting)",
      "requiredAtBoot": false
    },
    {
      "name": "LLM_API_KEY",
      "description": "API key for the LLM endpoint",
      "requiredAtBoot": false
    },
    {
      "name": "LLM_MODEL",
      "description": "Model name to use for extraction/drafting",
      "requiredAtBoot": false
    },
    {
      "name": "BRIGHT_DATA_API_KEY",
      "description": "Bright Data API token (Account settings → API tokens). Used by the SERP API (discovery) adapter.",
      "requiredAtBoot": false
    },
    {
      "name": "BRIGHT_DATA_SERP_ZONE",
      "description": "Bright Data SERP API zone name (Web Scraper API → SERP API zone, e.g. serp_api1). Used for new-service discovery.",
      "requiredAtBoot": false
    },
    {
      "name": "SCHEDULER_SECRET",
      "description": "Shared bearer token used only by the Base44 scheduled functions when calling the updater and discovery routes.",
      "requiredAtBoot": true
    },
    {
      "name": "WORKLOU_APP_BASE_URL",
      "description": "Published HTTPS base URL of this WorkLou app. Used by Base44 scheduled functions to call the protected job routes.",
      "requiredAtBoot": false
    }
  ]
}
